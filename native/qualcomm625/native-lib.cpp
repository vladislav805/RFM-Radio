#include <jni.h>
#include <string>

extern "C" {

	/*JNIEXPORT jint JNICALL Java_com_vlad805_fmtest_MainActivity_set_priority(JNIEnv* env, jobject thiz, jint new_priority) {
		int priority = 0;
		priority = getpriority(PRIO_PROCESS, 0);

		priority = setpriority(PRIO_PROCESS, 0, new_priority);

		priority = getpriority(PRIO_PROCESS, 0);
		return 0;
	}*/

}